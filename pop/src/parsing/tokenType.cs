namespace src.parsing
{
    public enum tokenType
    {
        identify,
        identity,
        let,
        sudo,
        program,
        init,
        run,
        terminate,
        warn,
        leftBrace,
        rightBrace,
        semiColon,
        equals,
        halt,
        eof
    }
}