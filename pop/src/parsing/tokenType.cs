namespace src.parsing
{
    public enum tokenType
    {
        identify,
        let,
        sudo,
        program,
        init,
        run,
        terminate,
        warn, 
        leftBrace, 
        rightBrace, 
        semiColon, 
        equals
    }
}